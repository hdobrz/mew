import 'package:flutter/material.dart';

class AdoptionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('التبني'),
        backgroundColor: Colors.teal,
      ),
      body: const Center(
        child: Text('صفحة التبني'),
      ),
    );
  }
}
