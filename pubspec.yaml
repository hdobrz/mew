name: mew
description: تطبيق يهتم بالقطط يحتوي على أقسام تعليمية، تبني، منتجات، وعيادات.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  animations: ^2.0.7
  font_awesome_flutter: ^10.7.0
  url_launcher: ^6.0.20

  intl: any
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/diseases.png
    - assets/fungi.png
    - assets/kittens.png
    - assets/post_mating.png
    - assets/mating.png
    - assets/neutering.png
    - assets/vaccines.png
    - assets/behaviors.png
    - assets/hygiene.png
    - assets/nutrition.png
    - assets/male_female.png
    - assets/hybrid_cats.png
    - assets/pure_cats.png
    - assets/found_cat.png
    - assets/2.png
